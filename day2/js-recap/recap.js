// comments in JS with //
/* or multline
with */

// variables
let name = "Maaike";
const PI = 3.14;

// function
function doSomething(x, y) {
    return x + y;
}

// arrow functions
let x = (name) => "hello" + name;
x("Hugh");

// equivalent of
function sayHi(name) {
    return "hello" + name;
}

